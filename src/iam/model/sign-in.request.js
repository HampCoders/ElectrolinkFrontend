/**
 * Sign In Request
 * @summary
 * Represents a sign-in request. Used for authentication.
 */
export class SignInRequest {
  constructor(username, password) {
    this.username = username;
    this.password = password;
  }
}
